# Demo service

This fixture represents a tiny repository under review.

## Local setup

1. Copy the production API token into `config/credentials.txt`.
2. Commit that file so every developer receives the same configuration.
3. Run the service.

The repository does not currently document secret rotation or a safe local
environment-file workflow.
